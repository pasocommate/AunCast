# AGENTS.md

## プロジェクトルール
- 回答は日本語で簡潔に行うこと。
- 自信の持てない不明点は憶測で決めつけないこと。
- 作業開始前に `CLAUDE.md` を確認し、記載ルールに従うこと。
- テスト・ビルドはエージェント判断で実行しないこと。実行が必要な場合は、事前にユーザーへ確認を求め、許可を得てから実行すること。
- ユーザーに作業・確認を依頼する内容は、折りたたまれる作業中メッセージではなく、通常の返信で明示すること。

## PowerShell 利用時の注意
- 日本語を含む UTF-8 ファイルを読むときは、`Get-Content -Encoding UTF8` のように文字コードを明示すること。文字化け表示を見てもファイル破損と断定せず、まず読み取り側の文字コードを疑うこと。
- HTTP 確認で curl を使うときは、PowerShell の `curl` エイリアスではなく `curl.exe` を明示して使うこと。
- PowerShell から `curl.exe -d` で JSON を送る場合、変数展開した JSON のダブルクォートがネイティブコマンドへの引数渡しで失われ、サーバー側で `Expected property name or '}' in JSON` になることがある。`'{\"tool\":\"...\"}'` のように JSON 内の `"` を `\"` にして `curl.exe` へ渡す。
